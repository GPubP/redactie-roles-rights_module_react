import {
	useMySecurityRightsForSite,
	useMySecurityRightsForTenant,
	useSiteRoles,
	useUsers,
} from '../hooks';

export const hooks = {
	useMySecurityRightsForSite,
	useMySecurityRightsForTenant,
	useUsers,
	useSiteRoles,
};
