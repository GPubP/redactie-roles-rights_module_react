import {
	useMySecurityRightsForSite,
	useMySecurityRightsForTenant,
	useTenantRoles,
	useUsers,
} from '../hooks';

export const hooks = {
	useMySecurityRightsForSite,
	useMySecurityRightsForTenant,
	useUsers,
	useTenantRoles,
};
