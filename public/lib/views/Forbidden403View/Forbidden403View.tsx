import { Container } from '@acpaas-ui/react-editorial-components';
import React, { FC } from 'react';

const Forbidden403View: FC = () => {
	// TODO: translate the conent inside the h1
	return (
		<Container>
			<h1>403: Toegang geweigerd</h1>
		</Container>
	);
};

export default Forbidden403View;
